{{> partials/navbar }}
<div class="container vh-100" id="main">
       <h1 class="h5">{{> partials/title }}</h1>
    <hr>

    <div class="row">
        {{#each dataCard}}
            <div class="col-md-4">
                {{> partials/card
                    titulo = this.title
                    imagen = this.url
                    descripcion = this.description
                }}
            </div>
        {{/each}}
    </div>

</div>
{{> partials/footer }}